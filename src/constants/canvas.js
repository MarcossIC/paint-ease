// Constantes relacionadas con el canvas y su comportamiento
export const MAX_HISTORY_INDEX = 50;
export const CANVAS_MODES = {
  DRAW: 'draw',
  SELECT: 'select',
  MOVE: 'move',
};

// Configuración por defecto del canvas
export const CANVAS_DEFAULTS = {
  width: 800,
  height: 600,
  background: '#ffffff',
};

export const ROUGHNESS = {
  architect: 0,
  artist: 1,
  cartoonist: 2,
};
